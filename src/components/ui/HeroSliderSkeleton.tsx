const HeroSliderSkeleton = () => {
    return (
        <div className="relative overflow-hidden bg-background">
            <div className="w-full h-[400px] md:h-[500px] lg:h-[600px] animate-pulse bg-muted" />
        </div>
    );
};

export default HeroSliderSkeleton;
